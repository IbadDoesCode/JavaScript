const age = 28; // can use Let also

if (age > 18) {
    console.log("You're In...");
} else if (age === 18) {
    // used else if only to show the
    // use of '===' and 'else if'
    console.log("You are Just In...");
} else {
    console.log("You're NOT In...");
}
